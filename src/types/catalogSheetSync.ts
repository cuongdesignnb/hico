export type CatalogSheetSyncField = 'price' | 'wmproductId' | 'apn' | 'networkLabel' | 'publicNote';
export interface CatalogSheetSyncRow { id: string; sheetRowNumber: number; variantId: string | null; status: 'VALID' | 'INVALID' | 'APPLIED' | 'SKIPPED'; normalizedData: Record<string, unknown>; diff: Partial<Record<CatalogSheetSyncField, { before: unknown; after: unknown; changed: boolean }>>; errors: Array<{ code: string; field?: string }>; appliedFields: CatalogSheetSyncField[]; }
export interface CatalogSheetSyncBatch { id: string; status: string; spreadsheetId: string; sheetTab: string; sheetRange: string; sourceHash: string; catalogVersionId: string | null; summary: { total?: number; valid?: number; invalid?: number; changedFields?: number }; createdAt: string; validatedAt: string | null; appliedAt: string | null; rejectedAt: string | null; }
