import { useState } from 'react';
import { KeyRound, LogOut, ShieldCheck } from 'lucide-react';
import { SeoHead } from '../../seo/SeoHead';
import { defaultMetadata } from '../../seo/buildMetadata';
import { changeCustomerPassword } from '../../services/customerProfileApi';
import { useCustomerAuth } from '../../auth/customer/useCustomerAuth';
import { useCustomerSecurityEvents } from '../../hooks/customer/useCustomerSecurityEvents';
import { useCustomerSessions } from '../../hooks/customer/useCustomerSessions';
export const AccountSecurityPage = () => { const { csrfToken } = useCustomerAuth(); const { data: sessions, revoke, revokeAll } = useCustomerSessions(); const { data: events } = useCustomerSecurityEvents(); const [form, setForm] = useState({ current: '', next: '' }); const [message, setMessage] = useState(''); const submit = async (event: React.FormEvent) => { event.preventDefault(); try { await changeCustomerPassword(form.current, form.next, csrfToken); setForm({ current: '', next: '' }); setMessage('Mật khẩu đã được thay đổi.'); } catch (value) { setMessage(value instanceof Error ? value.message : 'Không thể thay đổi mật khẩu.'); } }; return <><SeoHead path="/tai-khoan/bao-mat" metadata={{ ...defaultMetadata(), title: 'Bảo mật | HICO eSIM', indexable: false }} noindex /><div className="account-page-heading"><div><p className="account-kicker">Bảo mật</p><h2>Phiên và bảo mật</h2><p className="account-lead">Kiểm soát các phiên đăng nhập và thay đổi mật khẩu.</p></div></div>{message && <p className="account-feedback" role="status"><ShieldCheck size={16} />{message}</p>}<div className="account-detail-grid"><form className="account-panel account-form" onSubmit={submit}><h3><KeyRound size={18} /> Đổi mật khẩu</h3><label>Mật khẩu hiện tại<input type="password" value={form.current} onChange={(event) => setForm({ ...form, current: event.target.value })} required /></label><label>Mật khẩu mới<input type="password" value={form.next} onChange={(event) => setForm({ ...form, next: event.target.value })} required minLength={12} /></label><button className="account-button account-button-primary" type="submit"><KeyRound size={16} /> Cập nhật mật khẩu</button></form><section className="account-panel"><div className="account-panel-heading"><h3>Phiên đăng nhập</h3><button className="account-button" type="button" onClick={() => void revokeAll()}><LogOut size={16} /> Thu hồi tất cả</button></div><div className="account-address-list">{sessions.map((session) => <div className="account-address-row" key={session.id}><div><strong>{session.current ? 'Phiên hiện tại' : 'Phiên khác'}</strong><span>Hoạt động gần nhất: {new Date(session.lastSeenAt).toLocaleString()}</span></div>{!session.current && <button className="account-icon-button" title="Thu hồi phiên" aria-label="Thu hồi phiên" onClick={() => void revoke(session.id)}><LogOut size={16} /></button>}</div>)}</div></section></div><section className="account-panel account-security-events"><h3><ShieldCheck size={18} /> Sự kiện bảo mật gần đây</h3>{events?.items.map((event) => <div className="account-security-event" key={event.id}><span>{event.type}</span><time dateTime={event.createdAt}>{new Date(event.createdAt).toLocaleString()}</time></div>)}</section></>;
};
